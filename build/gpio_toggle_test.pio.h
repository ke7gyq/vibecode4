// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ---------------- //
// gpio_toggle_test //
// ---------------- //

#define gpio_toggle_test_wrap_target 0
#define gpio_toggle_test_wrap 3
#define gpio_toggle_test_pio_version 1

static const uint16_t gpio_toggle_test_program_instructions[] = {
            //     .wrap_target
    0xe000, //  0: set    pins, 0
    0xbf42, //  1: nop                           [31]
    0xe001, //  2: set    pins, 1
    0xbf42, //  3: nop                           [31]
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program gpio_toggle_test_program = {
    .instructions = gpio_toggle_test_program_instructions,
    .length = 4,
    .origin = -1,
    .pio_version = gpio_toggle_test_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config gpio_toggle_test_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + gpio_toggle_test_wrap_target, offset + gpio_toggle_test_wrap);
    return c;
}

static inline void gpio_toggle_test_program_init(PIO pio, uint sm, uint offset, uint gpio) {
    pio_sm_config c = gpio_toggle_test_program_get_default_config(offset);
    // Configure GPIO as output
    sm_config_set_set_pins(&c, gpio, 1);
    pio_gpio_init(pio, gpio);
    pio_sm_set_consecutive_pindirs(pio, sm, gpio, 1, true);
    // Clock configuration for 150 MHz system clock
    // Each loop: 2 * (nop[31] + set) = 2 * 33 = 66 cycles
    // With clkdiv=1: 150 MHz / 66 ≈ 2.27 MHz toggle frequency
    // With clkdiv=2.27: 150 MHz / 2.27 / 66 ≈ 1.0 MHz toggle frequency
    // 
    // Adjust clkdiv for different target frequencies:
    // - clkdiv = 1.0   → ~2.27 MHz
    // - clkdiv = 2.27  → ~1.0 MHz
    // - clkdiv = 4.54  → ~0.5 MHz
    sm_config_set_clkdiv(&c, 1.0);  // Set to desired divider
    pio_sm_init(pio, sm, offset, &c);
}

#endif

